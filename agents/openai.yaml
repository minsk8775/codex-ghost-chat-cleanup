interface:
  display_name: "유령 채팅 정리"
  short_description: "Codex 유령 채팅을 판별하고 백업 후 안전하게 제거"
  default_prompt: "Use $codex-ghost-chat-cleanup to inspect and safely remove confirmed ghost chats."

policy:
  allow_implicit_invocation: true
