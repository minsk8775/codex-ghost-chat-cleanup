interface:
  display_name: "유령 채팅 정리"
  short_description: "유령 채팅을 제목(ID)로 확인 후 일괄 백업·정리"
  default_prompt: "Use $codex-ghost-chat-cleanup to show confirmed ghost chats as title (ID) and batch-delete only the set I approve."

policy:
  allow_implicit_invocation: true
